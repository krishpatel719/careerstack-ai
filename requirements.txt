fastapi
uvicorn[standard]
python-multipart
pydantic
pydantic-settings
python-dotenv
PyMuPDF
pdfplumber
docx2txt
sentence-transformers
rapidfuzz
httpx
tenacity
python-dateutil
groq
pyjwt
bcrypt
# Required for pydantic's EmailStr (used by UserCreate/UserLogin) to work
# at all -- pydantic raises at import time without it. Not explicitly
# requested alongside pyjwt/bcrypt, but strictly necessary for those models.
email-validator
# MongoDB Atlas persistence. Motor is intentionally not used: it is deprecated
# as of 2026 in favour of PyMongo's async API, while this compatibility facade
# keeps the existing synchronous service contracts during the migration.
pymongo>=4.11,<5
# In-memory MongoDB used only by the repository tests; never contacted by the app.
mongomock>=4.3,<5
pytest
